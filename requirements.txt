# INF6083 - Projet P2 : Recommandation basée sur le contenu
# Venv, Python 3.10–3.13. Linux/Windows. CUDA optionnel (PyTorch / RAPIDS).

# --- Calcul et données (precursor, notebook) ---
numpy>=1.24,<3
pandas>=2.0
polars>=1.0
scipy>=1.10
pyarrow

# --- Scikit-learn (notebook : TF-IDF, SVD, métriques) ---
scikit-learn>=1.3

# --- Notebook ---
jupyter>=1.0
notebook>=7.0

# --- Utilitaires ---
tqdm>=4.65
pyyaml>=6.0

# --- Optionnel : monitoring GPU / RAM (precursor, check_env) ---
nvidia-ml-py
psutil

# --- Optionnel : PyTorch (Tâche 3, modèles neuraux) ---
# CPU : pip install torch
# CUDA 12 : pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0
